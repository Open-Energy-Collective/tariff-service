Throwaway file to test whether Actions runs correctly under the `oec-agent` bot
account rather than the flagged `axilotl` account. Safe to ignore — this branch
and its PR will be deleted/closed once the test completes, 2026-08-12.
